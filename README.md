# Проект демонстрирует получение данных из внешнего API и сохранение в бд sqlite.

## 🔧 Требования

- Python 3.14+  
- pip

## Пояснения некоторых решений
- в качестве бд выбрана sqlite ради удобства локальной разработки и простоты разворачивания
- https://jsonplaceholder.typicode.com выбран в качестве api поскольку является простым и бесплатным
- в http клиенте реализован только метод get, который забирает данные из api, думаю для тестового достаточно
- в качестве метода аутентификации используется jwt access_token, думаю для тестового достаточно 


## 🚀 Установка

1. **Клонируйте репозиторий:**
- git clone https://github.com/DontParryMe/third_party_api.git
- cd third_party_api

2. **Создайте виртуальное окружение:**
- python -m venv .venv

3. **Активируйте виртуальное окружение:**
- macOS / Linux: source .venv/bin/activate
- Windows (cmd): .venv\Scripts\activate
- Windows (PowerShell): .venv\Scripts\Activate.ps1

4. **Установите зависимости:**
- pip install -r requirements.txt

5. **Запуск проекта:**
- создайте и заполните файл .env (для локальной проверки можно просто скопировать .env.example)
- uvicorn main:app --reload
- swagger будет доступен по http://127.0.0.1:8000/docs
